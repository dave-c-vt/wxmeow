"""
Integration tests for wxmeow weather application.

This module contains integration tests that test the interaction
between multiple components of the application. These tests:

- Test component interactions and data flow
- May require a running Flask application instance
- Test chart rendering, data processing pipelines
- Verify HTML generation and JavaScript integration
- May use external APIs in controlled test environments

Integration tests are more complex than unit tests but should
still complete within a reasonable time frame (< 30 seconds each).
"""
