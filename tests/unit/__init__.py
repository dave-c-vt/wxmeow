"""
Unit tests for wxmeow weather application.

This module contains unit tests that test individual components
and functions in isolation. These tests should:

- Run quickly (< 1 second each)
- Not depend on external services
- Not require a running Flask application
- Test single functions or methods
- Use mocking for external dependencies

Test classes should inherit from unittest.TestCase or use pytest conventions.
"""
